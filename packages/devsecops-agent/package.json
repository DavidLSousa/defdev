{
  "name": "@defdev/devsecops-agent",
  "version": "1.0.0",
  "description": "DevSecOps agent — SAST, secrets detection and SCA for TypeScript/JavaScript",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@defdev/core": "^1.0.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
