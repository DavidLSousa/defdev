{
  "name": "@defdev/cli",
  "version": "1.0.0",
  "description": "defdev — security scanning CLI for code (DevSecOps) and cloud infrastructure (CSPM)",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "defdev": "./dist/bin/security-mvp.js"
  },
  "files": ["dist"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@defdev/core": "^1.0.0",
    "@defdev/devsecops-agent": "^1.0.0",
    "@defdev/cspm-agent": "^1.0.0",
    "chalk": "^5.3.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
