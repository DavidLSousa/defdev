{
  "name": "@defdev/github-action",
  "version": "1.0.0",
  "description": "defdev GitHub Action — automated security scanning on pull requests",
  "license": "MIT",
  "private": true,
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build && ncc build dist/index.js -o action-dist",
    "test": "vitest run",
    "clean": "rm -rf dist action-dist *.tsbuildinfo"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@defdev/core": "^1.0.0",
    "@defdev/devsecops-agent": "^1.0.0",
    "@defdev/cspm-agent": "^1.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "vitest": "^1.6.0"
  }
}
